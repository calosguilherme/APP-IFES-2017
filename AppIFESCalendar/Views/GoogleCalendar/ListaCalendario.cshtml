@model AppIFESCalendar.Models.GoogleCalendar
    
@{
    ViewBag.Title = "ListaCalendario";
}

<h2>Lista Calendario</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Calendarios)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Calendarios)
        </th>
        <th></th>
    </tr>
  
    @foreach (var item in Model.Calendarios.Calendars.Get.)
    {
    <tr>
        <td>
            @Html.DisplayFor(model => model.Credencial.UserId)
        </td>
        <td>
            @Html.DisplayFor(model => model.Credencial.Token)
        </td>
    </tr>
    }

</table>

